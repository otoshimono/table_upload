require "spec_helper"

describe TableUpload do
  it "has a version number" do
    expect(TableUpload::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
